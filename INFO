modification d'une branche:
Tout sur VScode:
    - onglet source control
    - appuyer sur le + a coté de "changes"
    - mettre un message de commit
    - appuyer sur commit
    - appuyer sur sync-commit

Voir les branches locales:
    - git branch

Voir les branches distantes:
    - git branch -r

Commande pour changer de branche:
    - git checkout nom_de_branche (exemple git checkout Abel ou git checkout main)

Créer une branche locale qui pointe sur une branche distante:
    - git checkout -b nom_de_branche origin/nom_de_branche (exemple git checkout -b Abel origin/Abel)


Mettre a jour les modifs github puis les recuperer en local:
    - git fetch origin
    - git pull origin nom_de_branche (exemple git pull origin Abel

EXEC=GestionRessources
CC=gcc
CFLAGS=-Wall -Wextra -Werror -g -Iinclude

# Fichiers sources
SRCS=src/main.c src/ui.c src/process.c src/manager.c src/network.c

# Transformation des fichiers sources en fichiers objets
OBJS=$(SRCS:.c=.o)

all: $(EXEC)
# création de l'exécutable
$(EXEC): $(OBJS)
	$(CC) $(CFLAGS) -o $@ $^

# création des fichiers objets
%.o: %.c
	$(CC) $(CFLAGS) -c $< -o $@

#Dépendances
src/main.o: src/main.c include/ui.h include/process.h include/manager.h include/network.h
src/ui.o: src/ui.c include/ui.h include/process.h
src/process.o: src/process.c include/process.h
src/manager.o: src/manager.c include/manager.h include/process.h include/ui.h include/network.h
src/network.o: src/network.c include/network.h

clean:
	rm -f $(OBJS) $(EXEC)



