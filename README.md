# Projet LP25

Il est nécessaire de télécharger la bibliothèque ncurses avant de lancer make
avec la commande:  sudo apt install libncurses5-dev libncursesw5-dev

Cette bibliothèque permet de créer une meilleure interface graphique.

